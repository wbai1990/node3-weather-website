<!DOCTYPE html>
<html>
    <header>
        <title>404</title>
        <link rel="stylesheet" href="css/styles.css">
    </header>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>